/**
 * Design System Tokens
 *
 * Central export of all design tokens (colors, spacing, typography, shadows).
 * These are the source of truth for the design system and are consumed by
 * Tailwind config, components, and applications.
 */

export { colors, type ColorToken } from "./colors"
export { typography, type TypographyToken } from "./typography"
