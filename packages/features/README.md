# Feature packages

A feature owns its schemas, types, server logic, components, and hooks as one vertical capability. Features may consume shared, platform, and UI packages; they must not depend on applications or casually depend on other features.
