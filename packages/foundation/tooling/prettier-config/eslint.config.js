// ESLint config for prettier-config package
// This package primarily exports configs and doesn't have source files to lint
export default [
  {
    ignores: ['node_modules/**', 'dist/**', '*.{js,mjs,json}'],
  },
]
