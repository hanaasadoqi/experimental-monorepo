# Testing packages

- Testing Library/Vitest behavior belongs in `vitest-utils`
- Runner-neutral browser substitutes in `test-mocks`. Production runtime source must not import this category.

## Planned

- Storybook behavior in `storybook-utils`
