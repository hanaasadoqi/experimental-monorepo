# Platform packages

Technical adapters for auth, databases, API clients, storage, email, queues, and observability belong here. Platform packages may consume shared contracts but must not depend on features or applications.
