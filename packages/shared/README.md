# Shared packages

Create a package here only for an environment-neutral contract with multiple legitimate owners. Do not extract feature code merely because it is a type, schema, utility, or hook. Shared packages may depend only on other lower-level shared contracts.

### Implemented

- utils

### Planned

- schemas
- types
- hooks
