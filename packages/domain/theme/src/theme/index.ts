/**
 * Theme: which colors constitute a theme, and per-scope overrides.
 *
 * Note the split from `../color`: that folder answers "what is a color",
 * this one answers "which colors make up a theme".
 */
export * from "./model"
