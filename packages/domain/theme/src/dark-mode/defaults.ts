export const DEFAULT_DARK_MODE_OPTIONS = {
  enableDarkMode: false,
}

export const DEFAULT_ENABLED_DARK_MODE_OPTIONS = {
  enableDarkMode: true,
  isDarkMode: false,
}

export const DEFAULT_IS_DARK_MODE = false as const
export const DEFAULT_IS_DARK_MODE_ENABLED = false as const
